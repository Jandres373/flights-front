"use client";

import { FaPlane } from "react-icons/fa";

type Props = {};

const Icon = (props: Props) => {
  return (
    <div className=" flex justify-items-center">
      <FaPlane className="text-lg text-black" />
    </div>
  );
};

export default Icon;
